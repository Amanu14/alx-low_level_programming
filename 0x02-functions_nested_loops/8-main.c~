#include <stdio.h>

/**
 * jack_bauer - Prints the minutes of a day
 *
 * Return: no return
 */

void jack_bauer(void)
{
int h1, h2, m1, m2;

for (h1 = 48; h1 <= 50; h1++)
{
for (h2 = 48; h2 <= 57; h2++)
{
for (m1 = 48; m1 <= 53; m1++)
{
for (m2 = 48; m2 <= 57; m2++)
{
if (h1 >= 50 && h2 >= 52)
break;
putchar(h1);
putchar(h2);
putchar(58);
putchar(m1);
putchar(m2);
putchar('\n');
}
}
}
}
}
