#include <stdio.h>
/**
 * main - Prints the alphabet without q and e.
 *
 * Return: Always 0 (Success)
 */
#include <stdio.h>
int main(void)
{
int i;
for (i = 1; i < 10; i++)
{
printf("%d",i);
}
printf("\n");
return (0);
}
