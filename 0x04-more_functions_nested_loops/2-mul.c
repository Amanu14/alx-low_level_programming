#include "main.h"

/**
 * mul - multiplication of two numbers.
 * @a: input number.
 * @b: another input number.
 * Return: always returns multiplication result.
 */

int mul(int a, int b)
{
return (a * b);
}
